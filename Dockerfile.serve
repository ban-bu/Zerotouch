# 备用Dockerfile - 使用serve的超简版本
FROM node:18-alpine

WORKDIR /app

# 安装curl
RUN apk add --no-cache curl

# 复制并安装依赖
COPY package*.json ./
RUN npm ci

# 构建应用
COPY . .
RUN npm run build

# 暴露端口
EXPOSE 8080

# 启动serve
CMD ["npx", "serve", "-s", "dist", "-l", "8080"]
